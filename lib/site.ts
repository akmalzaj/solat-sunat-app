// Canonical production origin. The primary domain is the apex (solat.wiki);
// www 301s to it on Netlify. Every absolute URL in metadata, the sitemap, and
// robots.txt is sourced from here so a domain change is a one-line edit.
export const SITE_URL = "https://solat.wiki";